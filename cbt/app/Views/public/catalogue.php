<section class="public-catalogue">
 <div class="public-cbt-hero"><div><span class="eyebrow">LIBERTY CBT</span><h1>Choose your exam. Practise with confidence.</h1><p>Browse available subjects, exam schedules and enrollment fees. Login is required only when you enroll.</p></div><div class="public-hero-stat"><b><?=count($tests)?></b><span>Available tests</span></div></div>
 <form class="card public-filter" method="get" action="/cbt/catalogue"><label>Search<input name="q" value="<?=e($search)?>" placeholder="Search test, exam or subject"></label><label>Exam<select name="exam"><option value="">All exams</option><?php foreach($exams as $name):?><option value="<?=e($name)?>" <?=$exam===$name?'selected':''?>><?=e($name)?></option><?php endforeach;?></select></label><label>Subject<select name="subject"><option value="0">All subjects</option><?php foreach($subjects as $item):?><option value="<?=e($item['id'])?>" <?=$subject===(int)$item['id']?'selected':''?>><?=e($item['name'])?></option><?php endforeach;?></select></label><button class="btn">Find exams</button></form>
 <div class="section-head"><div><span class="eyebrow">AVAILABLE NOW</span><h2>Subjects and mock tests</h2><p>Fees and schedules are shown before login.</p></div></div>
 <?php if($tests):?><div class="test-grid public-test-grid"><?php foreach($tests as $test) \App\Core\View::partial('student/test-card',compact('test'));?></div><?php else:?><div class="card empty-state"><h2>No matching exams found</h2><p>Try another exam, subject or search term.</p></div><?php endif;?>
</section>
